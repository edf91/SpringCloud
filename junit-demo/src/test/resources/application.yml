spring:
  application:
    name: juniDemo
  datasource:
    platform: h2
    schema: classpath:schema.sql
    data: classpath:data.sql
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    generate-ddl: false
server:
  port: 40000